================================================================================
  PRE-LAUNCH CHECKLIST — Meebits Futbol Bracket Challenge
  Last updated: April 21, 2026
  Tournament starts: June 11, 2026 (12:00 EDT / 16:00 UTC)
  Production URL:    https://meebits-futbol.vercel.app
================================================================================

CRITICAL — must be resolved before accepting real user submissions
================================================================================

[ ] Swap local JSON data store for a production database
    - Current: src/lib/db/index.ts re-exports `localStore` from localStore.ts,
      which writes to <project-root>/data/store.json.
    - Problem: Vercel's function filesystem is ephemeral — every new
      deployment wipes the JSON file and every submission is lost.
    - Fix: Implement the DataStore interface (src/lib/db/types.ts) against
      a real backend (Vercel Postgres, Neon, Supabase, Upstash, etc.) and
      change the one import line in src/lib/db/index.ts.
    - All callers (/api/submit, /api/leaderboard, /api/divisions,
      /api/cron/update-scores) go through the DataStore interface, so the
      swap is confined to a single file.

[ ] Set real prize amounts for each division
    - Current: src/lib/divisions.ts has `prize: 'TBD'` for all four tiers
      (Gold, Silver, Bronze, Free). This string renders literally on
      /divisions and in the division cards.
    - Fix: Update the `prize` field for each entry in the DIVISIONS array,
      or — if prizes are not finalized — replace "TBD" with a friendlier
      placeholder like "Prizes announced soon".

[ ] Proxy the Alchemy API key through a server-side API route
    - Current: NEXT_PUBLIC_ALCHEMY_API_KEY is exposed to the browser bundle,
      so anyone can read it out of the JS and use it against our quota.
    - Fix: Move the getNFTsForOwner call in src/lib/web3/alchemy.ts behind
      a new /api/alchemy/tokens route. Keep the key server-only.

[ ] Resolve playoff placeholder teams in src/lib/tournament/teams.ts
    - As of today (April 21) the UEFA / Inter-Confederation playoffs are
      finished. Six team slots currently carry `isPlayoffWinner: true`
      with placeholder labels — Groups A, B, D, F, I, K.
    - Fix: Replace the placeholders with the actual playoff winners, drop
      the isPlayoffWinner flag, and set real flag codes.

[ ] Smoke-test the delegate.xyz v2 integration end-to-end
    - Use a real pair: cold wallet holds the Meebit Futbol NFTs, hot wallet
      is delegated via https://delegate.xyz/ (ALL / CONTRACT / ERC721
      delegation types all need to work).
    - Sign in with the hot wallet and verify:
        a) the delegated NFT count appears in useUserDivision
        b) the welcome modal shows "X owned + Y delegated"
        c) the user lands in the correct division
    - Code is live (src/lib/web3/delegateRegistry.ts + useDelegatedBalance.ts)
      but has never been exercised against the real on-chain registry.

[ ] Confirm the Meebit Futbol contract has live holders on mainnet
    - Contract: 0x8fBb231840BeDF8a49080Ac3001B9c97BF35f4E9
    - If the collection is not yet minted, every user lands in the Free
      tier and the division system is cosmetic only.
    - If mint is still pending, decide whether to launch anyway with
      only the Free division active, or delay until holders exist.

[ ] Sanity-check the tournament lock date
    - Hardcoded in src/lib/divisions.ts:
        TOURNAMENT_LOCK_DATE = new Date('2026-06-11T16:00:00Z');
    - After this moment, isTournamentLocked() blocks new submissions
      and division upgrades everywhere. Confirm with the team that
      noon-EDT on opening day is the intended cutoff.


IMPORTANT — should be resolved before a public launch push
================================================================================

[ ] Add rate limiting on public API routes
    - /api/submit     — prevent spam submissions
    - /api/odds       — prevent excessive Polymarket calls
    - /api/divisions  — low priority but good practice
    - Recommend Upstash Ratelimit + Vercel Edge Middleware.

[ ] Add React error boundaries around the heavy bracket components
    - BracketView, GroupStage, ThirdPlaceSelector
    - Prevents a single render failure from breaking the whole page.

[ ] SEO / social meta tags
    - Open Graph + Twitter card tags on every page
    - Page-specific titles and descriptions
    - Configure in src/app/layout.tsx and per-page `metadata` exports

[ ] Dynamic OG share image for /my-brackets/[id]
    - Render champion + division badge + username via @vercel/og
    - Used when users share their completed bracket

[ ] Analytics
    - Track: wallet connections, bracket submissions, division distribution,
      Polymarket API health, cron success/failure
    - Vercel Web Analytics or PostHog are both easy drop-ins.

[ ] Point the real custom domain (meebitsfutbol.com or similar)
    - Today the app is reachable at https://meebits-futbol.vercel.app
      (registered as a Project Domain, auto-advances on every deploy).
    - For marketing a real TLD is probably wanted. Add it in
      Vercel Project Settings -> Domains and update NEXT_PUBLIC_APP_URL.


NICE TO HAVE — post-launch improvements
================================================================================

[ ] Unit tests for tournament logic
    - lib/tournament/r32Seeding.ts  (bracket generation + propagation)
    - lib/tournament/thirdPlace.ts  (3rd-place eligibility)
    - lib/divisions.ts              (tier assignment from NFT count)

[ ] Division upgrade flow
    - /api/submit already returns a 409 with the message "Use the upgrade
      endpoint to change divisions." — but that endpoint does not exist.
    - Build: POST /api/submit/upgrade — re-reads NFT count, updates the
      submission's division, locks any newly counted token IDs.
    - UI: "Upgrade Division" button on /my-brackets.

[ ] Admin dashboard
    - View all submissions, monitor division distribution, manual token
      unlocking if a wallet gets compromised.

[ ] Export / backup submissions
    - Scheduled dump to CSV for prize distribution and disaster recovery.


DONE — already landed (do not worry about these)
================================================================================

[x] Leaderboard page (/leaderboard) with scored entries and division filters
[x] Daily score-update cron wired up (vercel.json + /api/cron/update-scores)
[x] Delegate.xyz v2 support (code-complete — still needs smoke test, see above)
[x] Division system simplified to 4 tiers (Gold / Silver / Bronze / Free)
[x] Header wordmark logo (replaces square icon + text)
[x] Custom domain meebits-futbol.vercel.app configured as Project Domain
[x] All environment variables set in Vercel Production environment
[x] Flags rendered via <Flag> SVG component (works on Windows Chrome)
[x] Grey-text readability pass across every page
[x] Third-placed teams R32 assignment — replaced the greedy stand-in with
    the official FIFA 2026 Annexe C 495-row lookup table, parsed directly
    from the FIFA Regulations PDF and independently verified (every cell
    satisfies § 12.6 eligibility; every C(12,8) combination covered once).
    See src/lib/tournament/annexC.ts and scripts/parse-annex-c.py.


================================================================================
  End of Pre-Launch Checklist
================================================================================
